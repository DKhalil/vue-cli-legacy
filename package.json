{
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build --mode production --no-clean",
    "lint": "vue-cli-service lint",
    "watch": "vue-cli-service build --mode production --watch --no-clean"
  }
}
